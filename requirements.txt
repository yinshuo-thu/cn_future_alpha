# Python 3.12. Core pipeline (feature_model) needs only the first block.
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
scikit-learn>=1.3
lightgbm>=4.0
scipy>=1.10
pyyaml>=6.0
matplotlib>=3.7
# Deep-learning route (end_to_end) only:
torch>=2.1
